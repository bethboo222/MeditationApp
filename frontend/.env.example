# Google Sheets Apps Script URL (for questionnaire data collection)
VITE_SHEETS_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec

# API server URL — only needed in production (leave empty for local dev)
# Set this to your deployed server URL, e.g. https://your-server.render.com
VITE_API_BASE_URL=
